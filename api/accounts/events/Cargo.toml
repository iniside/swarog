[package]
name = "accountsevents"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# The published event vocabulary of the "accounts" domain (Go's
# api/accounts/accountsevents): the player.registered payload + its descriptor.
# Anyone who reacts to player lifecycle imports this; nobody imports the accounts
# implementation. Imports only the `bus` leaf.
[dependencies]
bus = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
