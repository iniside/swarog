[package]
name = "mailrpc"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# `mailrpc` — mail's minimal <name>rpc glue crate, re-exporting adminrpc's admin fan-out
# registration so the `mail` module never imports a foreign rpc crate directly (rule 3).
[dependencies]
adminrpc = { workspace = true }
