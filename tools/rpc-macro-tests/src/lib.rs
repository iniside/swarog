//! Harness crate for the `#[rpc]` proc-macro. The behaviour lives in
//! `tests/roundtrip.rs`; this lib target exists only so the crate is a valid
//! workspace member. See that test file for the sample trait and the end-to-end
//! proofs.
