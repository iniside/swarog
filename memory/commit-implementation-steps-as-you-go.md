# Commit implementation steps as they are completed

For an approved implementation plan, commit each completed and main-agent-reviewed
step immediately instead of waiting until the entire rollout is finished. Use the
plan's Conventional Commit scope, keep tests serialized, and never bundle unrelated
steps into one large final commit. The user explicitly prefers ongoing granular
history in this solo repository.
