# Binaries
/server
*.exe
*.exe~
/bin/
/dist/
/target/
# weles orchestrator artifact staging dir (`weles deploy`)
/deploy/

# Test / coverage
*.out
coverage.*

# Local secrets / env
.env.local
.env*.local
nocommit/

# Design assets — large binaries (keep the GameOps spec html/js as reference)
/Admin portal game backend.zip
UILayout/uploads/
UILayout/.thumbnail

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp

# runtime logs / pids and built binaries (Go dual-mode run/ + Quarkus sketch run/)
/run/
